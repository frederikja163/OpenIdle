# The frontend image is built with Frontend/ as its context. node_modules is by
# far the biggest entry here: the image installs its own from bun.lock, and the
# host's copy carries native binaries built for the wrong platform.

node_modules
.svelte-kit
build
test-results
playwright-report

.git
.gitignore
.vscode
.claude

# Local secrets and overrides. .env.example is documentation and stays.
.env
.env.*
!.env.example

Dockerfile
.dockerignore
*.md
